import { SortByDataPipe } from './sort-by-data.pipe';

describe('SortByDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByDataPipe();
    expect(pipe).toBeTruthy();
  });
});
